# SV Code Advisory — Example Site

This is a small Vite + React + Tailwind project containing the `App` component you provided.

Quick start

```bash
npm install
npm run dev
```

Open http://localhost:5173 to view the site.

Notes

- The project uses Tailwind CSS. After installing dependencies the dev server will process Tailwind.
- Paste your GHL iframe snippets into the Contact section in `src/App.jsx` where indicated.
